<h2>My Tasks</h2>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
          <%= "✔️" %>
      <% else %>
          <%= "✖️" %>
      <% end %>
      <%= link_to task.title, task_path(task) %>
      </li>
  <% end %>
</ul>

<%= link_to "create new task", new_path %>
